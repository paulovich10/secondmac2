export class Modelo {

    id: number;
    productoId: number;
    nombre: string;


    constructor (id, productoId, nombre) {
        id = id;
        productoId = productoId;
        nombre = nombre;
    }
}