export class Procesador {

    id: number;
    nombre: string;
    modeloId: number[];


    constructor(id, nombre, modeloId) {
        id = id;
        nombre = nombre;
        modeloId = modeloId;
    }
}